# 宿主机映射端口（由验证环境注入；本地可改）
CC_PUBLISH_PORT=3071
# PostgreSQL（compose 内网，不发布到宿主）
POSTGRES_USER=lux
POSTGRES_PASSWORD=luxpass
POSTGRES_DB=luxconsign
# JWT 密钥
JWT_SECRET=please-change-this-secret
# 启动时自动建表并写入演示数据
SEED_ON_START=true
